# UI Components

This folder contains reusable presentational/UI components managed by Radix and Tailwind.
You can install Shadcn components using the CLI:

```bash
npx shadcn-ui@latest add <component-name>
```

Example:
```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add dialog
```
